<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container">
        <form class="mt-3 w-50 mx-auto" action="/updateMovieData/<%= data.id%>" method="post">
            <div class="mb-3">
                <label for="title" class="form-label">Movie Name</label>
                <input type="text" class="form-control" id="title" name="title" value="<%= data.title %>">
                <input type="hidden" name="editId" value="<%= data.id %>">
            </div>
            <div class="mb-3">
                <label for="discription" class="form-label">Discription</label>
                <input type="text" class="form-control" id="discription" name="discription" value="<%= data.discription %>">
            </div>
            <div class="mb-3">
                <label for="relisDate" class="form-label">RelisDate</label>
                <input type="date" class="form-control" id="relisDate"  name="relisDate" value="<%=  data.relisDate %>">

            </div>
            <div class="mb-3">
                <label for="img" class="form-label">Movie Image</label>
                <input type="url" class="form-control" id="img" name="img"  value="<%= data.img %>">
            </div>
            <div class="mb-3">
                <label for="rating" class="form-label">Rating</label>
                <select name="rating" id="rating" class="w-100">
                    <option selected disabled>Rate the movie</option>
                    <option value="⭐"<% if(data.rating == '⭐'){%>selected<% } %> >⭐</option>
                    <option value="⭐⭐"<% if(data.rating == '⭐⭐'){%>selected<% } %>>⭐⭐</option>
                    <option value="⭐⭐⭐"<% if(data.rating == '⭐⭐⭐'){%>selected<% } %>>⭐⭐⭐</option>
                    <option value="⭐⭐⭐⭐"<% if(data.rating == '⭐⭐⭐⭐'){%>selected<% } %>>⭐⭐⭐⭐</option>
                    <option value="⭐⭐⭐⭐⭐"<% if(data.rating == '⭐⭐⭐⭐⭐'){%>selected<% } %>>⭐⭐⭐⭐⭐</option>

                </select>
            </div>
            <button type="submit" class="btn btn-primary mt">Update</button>
        </form>
    </div>

</body>
</html>